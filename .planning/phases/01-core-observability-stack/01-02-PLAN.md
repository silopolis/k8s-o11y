---
phase: 01-core-observability-stack
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - helmfile.yaml
  - values/kube-prometheus-stack.yaml
  - values/prometheus.yaml
  - values/grafana.yaml
  - values/alertmanager.yaml
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04
  - MON-05
  - MON-06
user_setup: []

must_haves:
  truths:
    - Prometheus is collecting metrics from node-exporter and kube-state-metrics
    - Grafana is accessible via NodePort
    - Alertmanager is receiving alerts from default PrometheusRules
    - Prometheus has retention configured (2-3 days, 2GB limit)
    - etcd monitoring is disabled (Talos compatibility)
  artifacts:
    - path: values/kube-prometheus-stack.yaml
      provides: Main chart values with Talos customizations
      min_lines: 50
    - path: values/prometheus.yaml
      provides: Prometheus-specific configuration
      min_lines: 30
    - path: values/grafana.yaml
      provides: Grafana configuration with NodePort
      min_lines: 20
    - path: values/alertmanager.yaml
      provides: Alertmanager configuration
      min_lines: 15
  key_links:
    - from: helmfile.yaml
      to: values/*.yaml
      via: values array in release definition
      pattern: "values:\\s*- values/"
    - from: values/prometheus.yaml
      to: node-exporter and kube-state-metrics
      via: serviceMonitorSelectorNilUsesHelmValues
---

<objective>
Deploy kube-prometheus-stack main chart with Talos-compatible configuration.

Purpose: Core observability infrastructure — Prometheus, Grafana, Alertmanager, node-exporter, kube-state-metrics — operational and collecting metrics.
Output: All monitoring components deployed and healthy, Grafana accessible, metrics flowing
</objective>

<execution_context>
@/home/tarax/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/tarax/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-observability-stack/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/phases/01-core-observability-stack/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Configure Helmfile for main deployment</name>
  <files>helmfile.yaml</files>
  <action>
    Update helmfile.yaml to enable kube-prometheus-stack deployment with dependency on CRDs.
    
    The helmfile should now have:
    1. CRD release (already deployed in Plan 01)
    2. Main kube-prometheus-stack release with:
       - "needs" dependency on kube-prometheus-stack-crds
       - Namespace: monitoring
       - Chart: prometheus-community/kube-prometheus-stack
       - Version: ~82.10.x (match research findings)
       - Values files split by concern
    
    Updated structure:
    ```yaml
    releases:
      - name: kube-prometheus-stack-crds
        # ... (existing from Plan 01)
      
      - name: kube-prometheus-stack
        namespace: monitoring
        createNamespace: true
        chart: prometheus-community/kube-prometheus-stack
        version: ~82.10.0
        needs:
          - kube-prometheus-stack-crds  # Wait for CRDs
        values:
          - values/kube-prometheus-stack.yaml
          - values/prometheus.yaml
          - values/grafana.yaml
          - values/alertmanager.yaml
        hooks:
          - events: ["presync"]
            showlogs: true
            command: "kubectl"
            args: ["wait", "--for", "condition=Established", "crd", "prometheuses.monitoring.coreos.com", "--timeout", "60s"]
          - events: ["postsync"]
            showlogs: true
            command: "kubectl"
            args: ["wait", "--for", "condition=ready", "pod", "-l", "app.kubernetes.io/name=prometheus", "-n", "monitoring", "--timeout", "300s"]
    ```
  </action>
  <verify>helmfile -f helmfile.yaml deps</verify>
  <done>Helmfile updated with main release and dependencies configured</done>
</task>

<task type="auto">
  <name>Create main values file with Talos customizations</name>
  <files>values/kube-prometheus-stack.yaml</files>
  <action>
    Create values/kube-prometheus-stack.yaml with high-level configuration:
    
    Key settings:
    - **Organization label**: Use {{ .Values.organization }} or $ORGANIZATION env var
      ```yaml
      commonLabels:
        organization: "{{ .Values.organization }}"
      ```
    
    - **Namespace override**: monitoring namespace
    
    - **Service monitor selector**: Allow ServiceMonitors from any source
      ```yaml
      prometheus:
        prometheusSpec:
          serviceMonitorSelectorNilUsesHelmValues: false
          podMonitorSelectorNilUsesHelmValues: false
    ```
    
    - **Alertmanager enabled**: true
    - **Grafana enabled**: true
    - **Node exporter enabled**: true (DaemonSet)
    - **Kube state metrics enabled**: true
    - **CoreDNS monitoring enabled**: true (as per context)
    
    - **Control plane components**:
      - etcd: disabled (MON-06 requirement)
      - kubeScheduler: enabled
      - kubeControllerManager: enabled
      - kubeProxy: enabled
    
    Leave detailed component configuration to specific values files (prometheus.yaml, grafana.yaml, etc.).
  </action>
  <verify>helmfile -f helmfile.yaml template --validate 2>&1 | head -50</verify>
  <done>Main values file created with Talos-compatible defaults</done>
</task>

<task type="auto">
  <name>Configure Prometheus with emptyDir storage</name>
  <files>values/prometheus.yaml</files>
  <action>
    Create values/prometheus.yaml with storage and retention configuration per CONTEXT.md decisions:
    
    ```yaml
    prometheus:
      prometheusSpec:
        # Storage: emptyDir for Talos Docker (not PVC)
        storageSpec:
          emptyDir:
            medium: ""
            sizeLimit: 2Gi
        
        # Retention: 2-3 days as decided
        retention: "3d"
        retentionSize: "2GB"
        
        # Resource limits (reasonable for single-node Talos)
        resources:
          requests:
            cpu: 100m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi
        
        # Enable features
        enableAdminAPI: false
        walCompression: true
        
        # Scrape configs (default is fine, but ensure node-exporter and kube-state-metrics are discovered)
        additionalScrapeConfigs: []  # Use ServiceMonitors instead
        
        # Organization label from env var
        externalLabels:
          organization: "${ORGANIZATION:-training}"
          cluster: "talos-local"
    ```
    
    Per CONTEXT.md decisions:
    - Storage type: emptyDir (not PVC) ✓
    - Retention: 2-3 days ✓
    - Size limit: 2GB ✓
    - etcd monitoring disabled in main values file ✓
  </action>
  <verify>helmfile -f helmfile.yaml template --selector name=kube-prometheus-stack 2>&1 | grep -A5 "retention"</verify>
  <done>Prometheus values configured with emptyDir storage and retention limits</done>
</task>

<task type="auto">
  <name>Configure Grafana with NodePort access</name>
  <files>values/grafana.yaml</files>
  <action>
    Create values/grafana.yaml with NodePort service for direct access:
    
    ```yaml
    grafana:
      enabled: true
      
      # Service: NodePort for direct access without ingress
      service:
        type: NodePort
        port: 80
        targetPort: 3000
        nodePort: 30030  # Within 30000-32767 range
      
      # Admin credentials (change in production!)
      admin:
        existingSecret: ""
        userKey: admin-user
        passwordKey: admin-password
      
      # Persistence: disabled (use default emptyDir)
      persistence:
        enabled: false
      
      # Resources
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi
      
      # Enable anonymous access for training environment
      grafana.ini:
        auth.anonymous:
          enabled: true
          org_role: Admin
        
        server:
          root_url: http://localhost:30030
      
      # Pre-installed dashboards (keep defaults)
      dashboards:
        default_home_dashboard_path: ""
    ```
    
    Per CONTEXT.md decisions:
    - Grafana access via NodePort ✓
    - Port in 30000-32767 range ✓
  </action>
  <verify>helmfile -f helmfile.yaml template --selector name=kube-prometheus-stack 2>&1 | grep -A10 "nodePort"</verify>
  <done>Grafana configured with NodePort 30030 for direct access</done>
</task>

<task type="auto">
  <name>Configure Alertmanager</name>
  <files>values/alertmanager.yaml</files>
  <action>
    Create values/alertmanager.yaml with basic configuration per CONTEXT.md:
    
    ```yaml
    alertmanager:
      enabled: true
      
      # Service: ClusterIP (internal only, no external access needed)
      service:
        type: ClusterIP
      
      # Storage: emptyDir (acceptable for training)
      alertmanagerSpec:
        storage:
          emptyDir:
            medium: ""
        
        # Resources
        resources:
          requests:
            cpu: 20m
            memory: 50Mi
          limits:
            cpu: 100m
            memory: 128Mi
      
      # Configuration: minimal receiver (null/webhook as per Claude's discretion)
      config:
        global:
          smtp_smarthost: null
          smtp_from: null
        
        route:
          receiver: 'null'
          group_by: ['alertname', 'severity']
          group_wait: 30s
          group_interval: 5m
          repeat_interval: 12h
        
        receivers:
          - name: 'null'
    ```
    
    Per CONTEXT.md: Alertmanager storage is emptyDir (acceptable for training) ✓
    Per CONTEXT.md: Default receiver to be decided by Claude — using 'null' receiver ✓
  </action>
  <verify>helmfile -f helmfile.yaml template --selector name=kube-prometheus-stack 2>&1 | grep -A5 "alertmanagerSpec"</verify>
  <done>Alertmanager configured with emptyDir and null receiver</done>
</task>

<task type="auto">
  <name>Deploy kube-prometheus-stack</name>
  <files>helmfile.yaml, values/*.yaml</files>
  <action>
    Deploy the main kube-prometheus-stack release with Helmfile.
    
    Command:
    ```bash
    helmfile -f helmfile.yaml sync --selector name=kube-prometheus-stack
    ```
    
    The deployment should:
    1. Wait for CRDs to be ready (via presync hook)
    2. Deploy Prometheus, Grafana, Alertmanager, node-exporter, kube-state-metrics
    3. Wait for pods to be ready (via postsync hook with kubectl wait)
    
    Timeout: 10 minutes (standard Helm timeout)
    
    If deployment fails:
    - Check pod status with kubectl describe
    - Check for resource constraints
    - Check if etcd monitoring was correctly disabled (no CrashLoopBackOff)
    
    Expected pods in monitoring namespace:
    - prometheus-kube-prometheus-stack-prometheus-0
    - alertmanager-kube-prometheus-stack-alertmanager-0
    - kube-prometheus-stack-grafana-xxx
    - kube-prometheus-stack-kube-state-metrics-xxx
    - kube-prometheus-stack-prometheus-node-exporter-xxx (DaemonSet, one per node)
  </action>
  <verify>
    kubectl get pods -n monitoring && \
    kubectl get svc -n monitoring | grep grafana && \
    kubectl get prometheus -n monitoring && \
    kubectl get alertmanager -n monitoring
  </verify>
  <done>kube-prometheus-stack deployed with all components running</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
    Deployed kube-prometheus-stack with:
    - Prometheus (emptyDir storage, 2-3 day retention, 2GB limit)
    - Grafana (NodePort 30030 for direct access)
    - Alertmanager (emptyDir, basic config)
    - node-exporter (DaemonSet)
    - kube-state-metrics
    - etcd monitoring disabled for Talos compatibility
  </what-built>
  <how-to-verify>
    1. Run: kubectl get pods -n monitoring
       Expected: All pods in Running state, no CrashLoopBackOff
    
    2. Run: kubectl get svc -n monitoring | grep grafana
       Expected: grafana service with NodePort 30030
    
    3. Access Grafana:
       - Get node IP: kubectl get nodes -o wide
       - Open browser: http://<node-ip>:30030
       - Should see Grafana login page (admin/admin or anonymous)
    
    4. Check Prometheus targets:
       - Port-forward: kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090 -n monitoring
       - Open: http://localhost:9090/targets
       - Should see node-exporter and kube-state-metrics as UP
    
    5. Verify etcd is NOT being scraped (no CrashLoopBackOff):
       - kubectl logs -n monitoring prometheus-kube-prometheus-stack-prometheus-0 | grep -i etcd
       - Should not show repeated failed scrapes
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `kubectl get pods -n monitoring` — all pods should be Running
2. Run `kubectl get svc -n monitoring` — grafana should have NodePort 30030
3. Access http://<node-ip>:30030 — Grafana should be accessible
4. Check Prometheus targets via port-forward — node-exporter and kube-state-metrics should be UP
5. Verify Alertmanager is in silenced state but operational
6. Check kubectl logs prometheus-xxx — no etcd-related errors (MON-06 verified)
</verification>

<success_criteria>
- All monitoring pods are Running in monitoring namespace
- Grafana is accessible via NodePort 30030
- Prometheus is collecting metrics from node-exporter and kube-state-metrics
- etcd monitoring is disabled (no CrashLoopBackOff)
- Retention is configured (2-3 days, 2GB limit)
- Default alerts are active in Alertmanager
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-observability-stack/01-02-SUMMARY.md`
</output>
