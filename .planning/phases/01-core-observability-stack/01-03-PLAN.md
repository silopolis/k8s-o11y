---
phase: 01-core-observability-stack
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - scripts/verify-phase1.sh
  - docs/phase1-access.md
autonomous: true
requirements:
  - MON-03
  - MON-04
user_setup: []

must_haves:
  truths:
    - Grafana shows pre-configured dashboards (nodes, pods, workloads)
    - Alertmanager is receiving alerts from default PrometheusRules
    - All success criteria from ROADMAP.md Phase 1 are verified
  artifacts:
    - path: scripts/verify-phase1.sh
      provides: Automated verification script
      min_lines: 40
    - path: docs/phase1-access.md
      provides: User documentation for accessing deployed services
      min_lines: 30
  key_links:
    - from: scripts/verify-phase1.sh
      to: kubectl
      via: cluster API queries
      pattern: "kubectl.*monitoring"
---

<objective>
Verify Phase 1 deployment and document access methods.

Purpose: Confirm all success criteria are met: Prometheus collecting, Grafana accessible with dashboards, Alertmanager receiving alerts.
Output: Verification report, documentation for accessing services
</objective>

<execution_context>
@/home/tarax/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/tarax/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-observability-stack/01-CONTEXT.md
@.planning/phases/01-core-observability-stack/01-01-PLAN.md
@.planning/phases/01-core-observability-stack/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create comprehensive verification script</name>
  <files>scripts/verify-phase1.sh</files>
  <action>
    Create verification script that checks all Phase 1 success criteria:
    
    Success Criteria from ROADMAP.md:
    1. Prometheus is collecting metrics from node-exporter and kube-state-metrics
    2. Grafana is accessible and shows pre-configured dashboards
    3. Alertmanager is receiving alerts from default PrometheusRules
    4. Prometheus has retention configured (2-3 days, 2GB limit)
    5. etcd monitoring is disabled (no CrashLoopBackOff)
    
    Script should verify:
    
    1. **Pod Health**:
       - All pods in monitoring namespace are Running
       - No pods in CrashLoopBackOff (especially Prometheus)
       - Node-exporter DaemonSet is running on all nodes
    
    2. **Service Accessibility**:
       - Grafana service exists with NodePort 30030
       - Prometheus service exists
       - Alertmanager service exists
    
    3. **Metrics Collection**:
       - Prometheus targets page shows node-exporter UP
       - Prometheus targets page shows kube-state-metrics UP
       - Sample metric query returns data (e.g., up{})
    
    4. **Dashboards**:
       - Grafana has pre-built dashboards (Node Exporter Full, Kubernetes / Compute Resources / Cluster, etc.)
    
    5. **Alerts**:
       - Alertmanager is receiving alerts (check /api/v1/alerts)
       - Default alerts are configured (check Prometheus /rules)
    
    6. **Configuration Verification**:
       - Prometheus retention is set correctly (3d, 2GB)
       - etcd is not being scraped (no errors in logs)
    
    Exit codes:
    - 0: All checks pass ✓
    - 1: Critical issues found ✗
    
    Output: Color-coded summary of pass/fail for each criterion
  </action>
  <verify>bash -n scripts/verify-phase1.sh</verify>
  <done>Verification script created and syntactically valid</done>
</task>

<task type="auto">
  <name>Run verification script</name>
  <files>scripts/verify-phase1.sh</files>
  <action>
    Execute the verification script to validate all Phase 1 success criteria.
    
    Command:
    ```bash
    bash scripts/verify-phase1.sh
    ```
    
    Parse the output and report:
    - Which criteria passed
    - Which criteria failed (if any)
    - Summary of issues found
    
    If verification passes (exit code 0):
    - Display success message
    - Note any warnings (non-critical issues)
    
    If verification fails (exit code 1):
    - Display specific failures
    - Suggest remediation steps
    - May need to go back to Plan 02 to fix issues
    
    Document the verification results in the summary.
  </action>
  <verify>bash scripts/verify-phase1.sh; echo "Exit code: $?"</verify>
  <done>Verification script executed and reported results</done>
</task>

<task type="auto">
  <name>Create access documentation</name>
  <files>docs/phase1-access.md</files>
  <action>
    Create user documentation for accessing deployed monitoring stack.
    
    Document:
    
    1. **Grafana Access**:
       - URL: http://<node-ip>:30030
       - How to find node IP: `kubectl get nodes -o wide`
       - Default credentials (admin/admin or anonymous access)
       - Pre-built dashboards available
    
    2. **Prometheus Access**:
       - Port-forward command: `kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090 -n monitoring`
       - URL: http://localhost:9090
       - Key pages: /targets, /graph, /rules
    
    3. **Alertmanager Access**:
       - Port-forward: `kubectl port-forward svc/kube-prometheus-stack-alertmanager 9093:9093 -n monitoring`
       - URL: http://localhost:9093
    
    4. **Useful Commands**:
       - Check pod status: `kubectl get pods -n monitoring`
       - View Prometheus logs: `kubectl logs -n monitoring prometheus-kube-prometheus-stack-prometheus-0`
       - List dashboards: `kubectl get configmap -n monitoring | grep grafana-dashboard`
    
    5. **Verification**:
       - Reference to verify script: `bash scripts/verify-phase1.sh`
    
    6. **Next Steps**:
       - Phase 2: Deploy Traefik Gateway API
       - Command: `/gsd-execute-phase 2` or `/gsd-plan-phase 2` if not yet planned
    
    Format as clear markdown with command examples.
  </action>
  <verify>cat docs/phase1-access.md | head -20</verify>
  <done>Access documentation created with all service endpoints documented</done>
</task>

<task type="auto">
  <name>Validate default dashboards</name>
  <files>docs/phase1-access.md</files>
  <action>
    Verify that Grafana has the expected pre-configured dashboards by querying the ConfigMaps.
    
    Command:
    ```bash
    kubectl get configmap -n monitoring -l grafana_dashboard=1
    ```
    
    Expected dashboards from kube-prometheus-stack defaults:
    - Node Exporter Full
    - Kubernetes / Compute Resources / Cluster
    - Kubernetes / Compute Resources / Namespace (Pods)
    - Kubernetes / Compute Resources / Node (Pods)
    - Kubernetes / Kubelet
    - Kubernetes / Networking / Cluster
    - Kubernetes / Persistent Volumes
    - Kubernetes / USE Method / Cluster
    - Kubernetes / USE Method / Node
    
    Document which dashboards are available in the access documentation.
    
    If dashboards are missing:
    - Check if grafana.sidecar.dashboards.enabled is true in values
    - Verify ConfigMaps have grafana_dashboard label
  </action>
  <verify>kubectl get configmap -n monitoring -l grafana_dashboard=1 2>/dev/null | wc -l</verify>
  <done>Dashboard validation complete, available dashboards documented</done>
</task>

<task type="auto">
  <name>Verify Alertmanager is receiving alerts</name>
  <files>scripts/verify-phase1.sh</files>
  <action>
    Verify that Alertmanager is operational and receiving alerts from Prometheus default rules.
    
    Check:
    1. Alertmanager pod is running
    2. Alertmanager API responds
    3. Default PrometheusRules exist
    4. At least one alert is configured (even if not firing)
    
    Commands:
    ```bash
    # Check Alertmanager pod
    kubectl get pods -n monitoring -l app.kubernetes.io/name=alertmanager
    
    # Check default rules
    kubectl get prometheusrules -n monitoring
    
    # Sample query to verify Prometheus is evaluating rules
    kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090 -n monitoring &
    curl -s http://localhost:9090/api/v1/rules | jq '.data.groups | length'
    ```
    
    Expected: Non-zero number of rule groups (default is ~10-15 groups with 50+ rules)
    
    Update verification script if needed to include alert validation.
  </action>
  <verify>curl -s http://localhost:9090/api/v1/rules 2>/dev/null | jq '.data.groups | length' || echo "Need port-forward"</verify>
  <done>Alertmanager alert reception verified</done>
</task>

<task type="auto">
  <name>Verify etcd monitoring disabled</name>
  <files>scripts/verify-phase1.sh</files>
  <action>
    Critical check: Verify etcd monitoring is disabled per MON-06 requirement.
    
    Check:
    1. Prometheus logs show no etcd scrape errors
    2. No etcd targets in Prometheus targets page
    3. Prometheus pod is not in CrashLoopBackOff
    
    Commands:
    ```bash
    # Check Prometheus pod status
    kubectl get pod -n monitoring -l app.kubernetes.io/name=prometheus
    
    # Check for etcd errors in logs
    kubectl logs -n monitoring -l app.kubernetes.io/name=prometheus --tail=50 | grep -i etcd || echo "No etcd errors found ✓"
    
    # Check Prometheus configuration (should not have etcd scrape configs)
    kubectl get secret -n monitoring prometheus-kube-prometheus-stack-prometheus -o jsonpath='{.data.prometheus\.yaml\.gz}' | base64 -d | gunzip | grep -i etcd || echo "No etcd in config ✓"
    ```
    
    Expected: No etcd errors, no etcd scrape configs, Prometheus pod stable
    
    If etcd errors are found:
    - Document in issues
    - May need to adjust values/kube-prometheus-stack.yaml to explicitly disable etcd
  </action>
  <verify>[[ $(kubectl get pod -n monitoring -l app.kubernetes.io/name=prometheus -o jsonpath='{.items[0].status.phase}') == "Running" ]] && echo "Prometheus is Running ✓"</verify>
  <done>etcd monitoring disabled verified, Prometheus stable</done>
</task>

<task type="auto">
  <name>Generate verification report</name>
  <files>scripts/verify-phase1.sh, docs/phase1-access.md</files>
  <action>
    Generate final verification report summarizing Phase 1 status.
    
    Report should include:
    
    1. **Success Criteria Status** (from ROADMAP.md):
       - [x] Prometheus collecting metrics from node-exporter ✓/✗
       - [x] Prometheus collecting metrics from kube-state-metrics ✓/✗
       - [x] Grafana accessible via NodePort 30030 ✓/✗
       - [x] Grafana shows pre-configured dashboards ✓/✗
       - [x] Alertmanager receiving alerts ✓/✗
       - [x] Prometheus retention configured (3d, 2GB) ✓/✗
       - [x] etcd monitoring disabled (no CrashLoopBackOff) ✓/✗
    
    2. **Deployment Summary**:
       - Helm release: kube-prometheus-stack
       - Namespace: monitoring
       - Services: grafana (NodePort 30030), prometheus, alertmanager
       - Pods: prometheus, alertmanager, grafana, kube-state-metrics, node-exporter
    
    3. **Requirements Covered**:
       - MON-01 through MON-06
    
    4. **Access Information**:
       - Reference docs/phase1-access.md
    
    5. **Known Issues/Warnings** (if any):
       - Document any non-critical warnings from verification
    
    6. **Next Phase**:
       - Phase 2: Traefik Gateway API
       - Dependency: This Phase 1 must be complete
    
    Save report to: .planning/phases/01-core-observability-stack/01-03-SUMMARY.md
  </action>
  <verify>ls -la .planning/phases/01-core-observability-stack/01-03-SUMMARY.md</verify>
  <done>Verification report generated with all success criteria status</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `bash scripts/verify-phase1.sh` — should pass all checks
2. Check docs/phase1-access.md exists and is comprehensive
3. Verify 01-03-SUMMARY.md is created with status
4. Confirm all MON-01 through MON-06 requirements are covered
</verification>

<success_criteria>
- All 5 Phase 1 success criteria verified and passing
- Verification script exists and passes
- Access documentation created
- Summary report generated
- Ready to proceed to Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-observability-stack/01-03-SUMMARY.md`
</output>
