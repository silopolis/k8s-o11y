---
phase: 01-core-observability-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helmfile.yaml
  - values/kube-prometheus-stack-crds.yaml
  - scripts/preflight.sh
autonomous: true
requirements:
  - MON-01
  - MON-02
user_setup:
  - tool: helm
    why: Required to deploy charts
    install: "curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash"
  - tool: helmfile
    why: Required for declarative deployments
    install: "https://helmfile.readthedocs.io/en/latest/#installation"

must_haves:
  truths:
    - CRDs are installed before main chart deployment
    - Preflight checks pass before any deployment
    - Helm repositories are configured
  artifacts:
    - path: helmfile.yaml
      provides: Helmfile with CRD release definition
      min_lines: 30
    - path: values/kube-prometheus-stack-crds.yaml
      provides: CRD values configuration
      min_lines: 10
    - path: scripts/preflight.sh
      provides: Cluster health verification script
      min_lines: 20
  key_links:
    - from: helmfile.yaml
      to: values/kube-prometheus-stack-crds.yaml
      via: values reference in releases section
      pattern: "values:\\s*- values/kube-prometheus-stack-crds"
---

<objective>
Deploy kube-prometheus-stack CRDs and configure Helm environment for Phase 1 foundation.

Purpose: CRDs must exist before the main kube-prometheus-stack chart can be deployed. This prevents race conditions and ensures clean installation.
Output: CRDs installed, Helm repos configured, preflight checks passing
</objective>

<execution_context>
@/home/tarax/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/tarax/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-observability-stack/01-CONTEXT.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Helmfile structure for CRDs deployment</name>
  <files>helmfile.yaml, values/kube-prometheus-stack-crds.yaml</files>
  <action>
    Create helmfile.yaml with two releases:
    1. "crds" release using prometheus-community/prometheus-operator-crds chart (latest stable)
    2. "kube-prometheus-stack" release (defined but commented out or with condition for Phase 1 Plan 02)

    The CRDs release should:
    - Use namespace "monitoring"
    - Create namespace if not exists
    - Have no dependencies (runs first)
    - Use minimal values

    Create values/kube-prometheus-stack-crds.yaml with:
    - Empty or minimal configuration (CRDs chart typically needs no customization)

    Repository configuration:
    - Add prometheus-community repository

    Structure:
    ```yaml
    repositories:
      - name: prometheus-community
        url: https://prometheus-community.github.io/helm-charts

    releases:
      - name: kube-prometheus-stack-crds
        namespace: monitoring
        createNamespace: true
        chart: prometheus-community/prometheus-operator-crds
        version: ~11.0.0  # Use latest stable 11.x
        values:
          - values/kube-prometheus-stack-crds.yaml

      - name: kube-prometheus-stack
        # Will be enabled in Plan 02 with "needs" dependency on CRDs
        installed: false  # Disabled for now
    ```
  </action>
  <verify>helmfile lint passes without errors</verify>
  <done>Helmfile structure created with CRD release defined and repository configured</done>
</task>

<task type="auto">
  <name>Create preflight checks script</name>
  <files>scripts/preflight.sh</files>
  <action>
    Create comprehensive preflight checks script at scripts/preflight.sh that verifies:

    1. kubectl is installed and configured
    2. Current context is valid and cluster is reachable
    3. Helm is installed
    4. Helmfile is installed
    5. Nodes are healthy and ready
    6. Sufficient resources available (CPU/memory)
    7. "monitoring" namespace doesn't already exist (or is empty)
    8. No conflicting CRDs from previous failed installs
    9. Organization environment variable is set (warn if missing)

    Exit codes:
    - 0: All checks pass
    - 1: Critical failure (stop deployment)
    - 2: Warnings only (proceed with caution)

    Output should be clear and color-coded where supported.
  </action>
  <verify>bash scripts/preflight.sh returns exit code 0 or 2</verify>
  <done>Preflight script exists and passes validation</done>
</task>

<task type="auto">
  <name>Run preflight checks</name>
  <files>scripts/preflight.sh</files>
  <action>
    Execute the preflight checks script to validate the environment is ready for deployment.

    If checks fail with exit code 1:
    - Display the specific failures
    - Provide remediation steps
    - Stop deployment

    If checks return exit code 2 (warnings):
    - Display warnings
    - Continue with user acknowledgment (if interactive) or proceed (if autonomous/YOLO mode)

    Document any warnings in the plan summary.
  </action>
  <verify>bash scripts/preflight.sh; echo "Exit code: $?"</verify>
  <done>Preflight checks executed and passed (allowing warnings)</done>
</task>

<task type="auto">
  <name>Deploy CRDs via Helmfile</name>
  <files>helmfile.yaml, values/kube-prometheus-stack-crds.yaml</files>
  <action>
    Deploy kube-prometheus-stack CRDs using Helmfile.

    Command:
    ```bash
    helmfile -f helmfile.yaml sync --selector name=kube-prometheus-stack-crds
    ```

    Or with specific release:
    ```bash
    helmfile -f helmfile.yaml -l name=kube-prometheus-stack-crds sync
    ```

    Verify deployment:
    - Check that Prometheus Operator CRDs exist
    - kubectl get crd | grep monitoring.coreos.com
    - Should show: alertmanagers, prometheuses, prometheusrules, servicemonitors, podmonitors, probes, alertmanagerconfigs, scrapeconfigs

    Timeout: 5 minutes (CRDs are lightweight)

    If deployment fails:
    - Check if CRDs already exist from previous deployment
    - If they exist and are compatible, proceed with warning
    - If they exist but are incompatible version, document the conflict
  </action>
  <verify>kubectl get crd alertmanagers.monitoring.coreos.com prometheuses.monitoring.coreos.com prometheusrules.monitoring.coreos.com servicemonitors.monitoring.coreos.com</verify>
  <done>Prometheus Operator CRDs are installed and visible in cluster</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `kubectl get crd | grep monitoring.coreos.com` — should show 8-10 CRDs
2. Run `helm list -n monitoring` — should show kube-prometheus-stack-crds release
3. Run `helmfile -f helmfile.yaml lint` — should pass
4. Verify scripts/preflight.sh is executable and functional
</verification>

<success_criteria>
- Helmfile structure exists with CRD release defined
- CRDs are successfully installed in the cluster
- Preflight checks script exists and is functional
- Helm repositories are configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-observability-stack/01-01-SUMMARY.md`
</output>
